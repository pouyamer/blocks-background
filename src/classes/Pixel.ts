class Pixel {
  originalX: number
  originalY: number
  hslColor: HslColor

  constructor(originalX: number, originalY: number, hslColor: HslColor) {
    this.originalX = originalX
    this.originalY = originalY
    this.hslColor = hslColor
  }
}
